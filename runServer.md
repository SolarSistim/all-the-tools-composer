# Terminal 1 — Angular app
  cd "C:\Users\cwmpe\Documents\Angular Projects\all-the-tools-composer"       
  npm start

  # Terminal 2 — Compose server (file read/write + Netlify deploy)
  cd "C:\Users\cwmpe\Documents\Angular Projects\all-the-tools-composer"       
  node scripts/compose-server.js

  Then open http://localhost:4200 — it redirects straight to /compose.